#include<bits/stdc++.h>
using namespace std;

void bubbleSort(int arr[], int size){

}

int main(){
    int arr[] = {2, 4, 10, 8, 3 , 12, 9};
    int len = sizeof(arr) / sizeof(arr[0]);
    bubbleSort(arr, len);

    return 0;
}